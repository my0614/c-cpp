#include <stdio.h>
#include <math.h>
int main()
{
	int n,i,h=0,t;
	scanf("%d",&n);
	t = sqrt(n);
	for(i=1;i<=t;i++)
	
        if(n%i==0)
        {
        	h=h+(i + n/i);
    		
		}
    if(t*t == n)
    	h=h-t;
    printf("%d",h);
	return 0;
}
